当前用户需求：
{{effective_query}}

执行要求： 默认基于 React + Vite + TypeScript 模板；如果体验明显受益，可以自行扩展。

输出方式：
1. 当前可写目录 `{{workspace_dir}}` 是这个样本固定的 `main/project` 工作目录
2. 所有项目文件都必须直接写入 `{{workspace_dir}}`。
3. 不要只给方案或伪代码，必须落地为实际文件。
