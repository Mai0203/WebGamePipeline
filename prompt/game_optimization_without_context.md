你正在基于已有版本继续打磨一个面向大众用户的 Web 小游戏。

当前用户实时需求：
{{effective_query}}

原始需求背景：
{{original_query}}

当前信息：
- data_id: {{data_id}}
- 当前轮次: {{round_name}}
- 当前可写目录: {{workspace_dir}}（固定的 `main/project` 工作目录）

说明：
1. 你已经在当前会话中拥有前面轮次的上下文，这个 prompt 不再重复附带全部历史信息。
2. 当前用户实时需求已经是基于全部历史反馈提炼出的新需求，你应围绕它继续优化。

优化要求：
1. 当前轮次必须继续在 `{{workspace_dir}}` 中优化出完整的最新版本；这个目录始终是当前样本固定的 `main/project` 工作目录。
2. 优先解决当前用户实时需求对应的核心问题，并兼顾视觉、性能、可玩性和整体完成度。
3. 如果你认为需要回看历史细节，可以自行读取当前样本目录下已有的历史产物文件。
4. 所有文件都必须继续写入 `{{workspace_dir}}`。
5. 如果引入新的依赖或脚本，必须同步更新对应配置文件。
6. 最终版本应当明显优于历史版本，并且更贴近当前用户实时需求。
